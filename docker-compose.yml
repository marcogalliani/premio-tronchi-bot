services:
  premio-tronchi:
    build: .
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/premio_tronchi.db
    volumes:
      - premio-tronchi-data:/data
    restart: unless-stopped

volumes:
  premio-tronchi-data:
